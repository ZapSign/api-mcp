# This generic file is intentionally not loaded by Wrangler or Vitest.
# For local Worker bindings, copy .dev.vars.example to .dev.vars.
# For explicit sandbox integration tests, copy test/.env.test.example to test/.env.test.
